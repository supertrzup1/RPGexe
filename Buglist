Current storage of bugs for the most recent edition of the game.

As such, a format is used for this file. It features the bug description/title first, the reproduction steps, then further details with
featured items and pieces to note within the bug.

Example is occuring. You do this by example-ing. It happens when I example and happens to example often. Located at example.
